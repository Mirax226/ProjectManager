# Manual Test Checklist

1) Add 3 projects → add env keys for each → redeploy → verify keys persist (Config DB).
2) List cron jobs → link to projects → filter by project → confirm results.
3) Set Telegram token for a project → set webhook → run test → confirm message received.
4) Verify non-admin users cannot access secrets menus.
